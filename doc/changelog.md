# 漢字超戦 変更履歴

■ kcv66 不具合修正・ラスボス軌道刷新・全体リファクタ
- 【重大】オープニング／リザルト画面でキーを押すと題名に留まらずゲームが始まる不具合を修正
  （keydownリスナーの連鎖が原因。e.__kcConsumedフラグで「消費済みイベント」を後段に伝達して解決）
- 自機の上端制約をデモ中（demoActive）のみ有効化（プレイ中は無効）
- デモのボス追従収束速度を0.12→0.35に引き上げ（ボスの真下へ追従できずボスを倒せなかった不具合の修正）
- ジョイスティック停止後に自機が一瞬上にずれる不具合を修正
  （ニュートラル復帰時にinput.activeをfalseへ戻していなかったことが原因）
- 題名画面の3文字が右へずれていた不具合を修正（textAlignの設定漏れ）
- 最終ボス爆散演出を強化（飛散速度7倍・画面外到達・寿命3倍・爆音3倍）
- クリア演出を3倍の時間（2000→6000ms）・螺旋軌道に変更
- ラスボス（ボス3）の軌道を画面全体を使うbossFinalMove()へ刷新
- VEZAR旋律（Q6・Q7・S2・S4）をkcv56版へ差し戻し
- 全体リファクタ：drawText()等への描画集約・ボス移動共通化・敵生成統合・
  keydownリスナー単一窓口化（4→2箇所、keydownHandlers[]配列管理）
  ※keydownHandlersの呼び出し順は①〜⑤の順で固定。変更厳禁

■ kcv67 クリア演出試作（廃止版）
- クリア演出をギャラガ風（敵が画面横から登場して整列）として試作
- ユーザー確認の結果イメージと異なるとの判断で廃止（コードは次版で差し替え）

■ kcv68 クリア演出刷新・ランキングフリーズ修正
- ランキングパネルの「タップで題名へ戻る」ヒントをrankPanel直下に移動し常時表示（フリーズ修正）
- クリア演出を全面刷新：KANJI_POOL全128種を8列×8体の縦編隊で画面下から上昇、
  各列がsin波で蛇行しながら上昇する「くねった曲線の隊列」方式に変更
- 全員が画面上端を抜けたあと2秒の余韻（clearAfterGlow）→リザルト遷移を追加

■ kcv69 クリア演出の曲線隊列化
- 整列を直線から曲線隊列に変更：行インデックスと時間の両方でsin波を合成し、
  列全体が常に蛇のようにくねりながら上昇する動きに改善
- 余韻の開始条件を「全員が画面上端を抜けたことをevery()で検知」する方式に変更
  （従来はタイマー固定だったため、演出が長くなると余韻が早まる問題があった）

■ kcv70 クリア演出の調整
- 2波目スポーン開始を1波目スポーンから2秒後→6.5秒後に変更（1波目と重なる問題の修正）
- 振幅を大幅拡大（0.28〜0.46→0.70〜1.10）、くねりが視覚的に明確になるよう調整
- 第一形態の編隊横移動加速倍率を16倍→32倍に強化（デモでボスを倒しやすくする対策も兼ねる）

■ kcv71 バージョン番号のみ更新（ユーザー側作業）
- ユーザー側でKCV_VERSIONとタイトルをkcv70→kcv71に書き換えのみ。中身はkcv70と同一

■ kcv71-01 デバッグ・構造化（Claude）
- 【バグ修正】ゲームパッドが「たまに変な動きになる」報告の真因候補を除去：
  gp.axes?.[0] / gp.axes?.[1] でオプショナルチェーンを追加。
  一部の非標準コントローラでgp.axesがnull/undefinedの場合TypeErrorが発生し、
  入力全体がおかしくなる可能性があった
- 【最適化】countDiving() O(n²)→O(n)：updateEnemies先頭で1回だけ全スキャン後に
  キャッシュし、hover→diving遷移時にインクリメントする方式に変更
- 【デッドコード除去】demoMoveTimer（宣言・リセットのみ、読み出しなし）
- 【デッドコード除去】lastBossChar（宣言・設定のみ、読み出しなし）
- 【デッドコード除去】spawnSingleEscortEnemy関数（呼び出し箇所なし、spawnApproachEnemyに置換済み）
- 【構造化】意図的に無視するcatch句を catch(_){} に統一（7箇所）。
  console.error()で使うものは変更なし

■ kcv72 マトリックス風星背景・初期実装（Grok）
- ステージ3以降（stageIndex≥2）、背景の星を半角カタカナ＋英数字に切り替えるマトリックス風演出を試作
  （文字がランダムに変化する方式。ユーザー確認後、kcv75で全面リデザイン）

■ kcv73 ランキング名入力中のキー誤動作修正（Grok）
- ランキング名入力中にA/S/D/W/Z/X等を押すとゲーム操作として誤検知される不具合を修正
  （keydownハンドラ①〜⑤の先頭にrankingFormActiveガードを追加、
  ①にはさらにINPUT/TEXTAREAタグチェックを追加）

■ kcv74 プレイヤー弾速強化（Grok）
- プレイヤー弾のvy:-7 → vy:-28（4倍速）。弾が遅く敵に届きにくい問題への対応

■ kcv75 統合版（Claude）
kcv71-01・kcv72-74を統合し、Matrix rainとワープ演出を全面的に再実装

- 【Matrix rainリデザイン】kcv72の簡易版を廃止し、縦カラム落下型の本格的なデジタルレインに刷新
  - 画面幅に応じた列数（charH=cell×0.82ピクセル幅で均等分割）のストリームを独立管理
  - 先頭文字：白みがかった明るい緑（#e0ffe0）で視認性を強調
  - トレイル：明るい緑→暗い緑へ指数的に減衰（α値で制御）
  - 文字はランダムなタイミングで個別に変化（50〜220ms間隔）
  - ストリームごとに長さ・速度・初期位置がランダムで、スタートアップ時も均一にならない
  - 文字プール：半角カタカナ＋漢字（一二三…電龍等、ゲームテーマと世界観を統一）
- 【ワープ演出】ステージ2ボス撃破→ステージ3突入時に3秒間のワープエフェクトを追加
  - 100本の光の筋が画面中央から放射状に射出、加速しながら画面外へ消えていく
  - 速度は指数的に増加（factor: 1 + t²×18）。終盤に淡い緑のフラッシュを追加
  - 一部（約12%）は白色の光線でアクセント。ワープ完了後にMatrix rainへ自動切替
  - リサイズ時にstageIndex≥2かつwarpActive=falseの場合、Matrixストリームを再初期化
- 【スクショキー除外】Alt/Ctrl/Shift/PrintScreen/Metaを「任意のキーで画面送り」（ハンドラ②）
  および「題名画面のゲーム開始」（ハンドラ⑤）から除外（ANYKEY_EXCLUDEセットで管理）。
  スクリーンショット撮影操作でゲームが誤動作する問題を解消
- kcv73のランキング入力キー修正・kcv74の弾速強化・kcv71-01の全修正を統合

■ kcv76 演出・BGM切り替え・漢字プール拡張（段階実装 Stage1+2）

Stage1（軽量変更3件）：
- Matrix rainの文字プールから漢字を除外し、半角カタカナ45文字のみに変更
  （漢字が敵文字と誤認される問題の解消）
- ボス3撃破時にstopSong()を追加（クリア演出は無音で進行）
- プロムナードをステージ2開始時のみ再生するよう変更
  （ステージ3突入時はDash!ループに切り替え予定→kcv77で実装）

Stage2（中量変更2件）：
- KANJI_POOL: 128文字→256文字に拡張（追加128文字の内訳：自然・季節20、人・社会20、動作・変化20、
  抽象・感情20、色・素材12、学習・文化12、身体12、場所・建物12）
- クリア演出: 1波あたり8列×8行（64文字）→8列×16行（128文字）、2波合計256文字に拡張
  タイミング定数更新：CLEAR_WAVE2_DELAY_MS 6500→9000ms、CLEAR_CELEBRATION_MS 13000→20000ms

■ kcv77 Dash! フルループ BGM実装（Stage3完了）

- Dash!（© 1994-2026 MOTOI Kenichi）をVEZARエンジンと同一方式（scheduleSong + loop=true）で実装
  BASICリスト（PC-E500 PLAY3 MML）のセクションA→B→(RE+C×2+C2+D×2+E)×3を
  64セグメントのフラット配列（DASH_LOOP_SEGMENTS）に変換、DASH_LOOP_TEMPO=150（BASICリストT150に準拠）
  ループ時はセグメント先頭（セクションA）に自動復帰
- ステージ3突入（ボス2撃破）時にplayDashLoop()を呼び出し
  （ワープ演出と同時に開始、ボス3撃破でstopSong()）
- BASICリストのMML解釈注記：
  - len=0グレースノート（C0/D0/E0/F0/G0）はLENGTH_TABLE[0]=3で処理、パーサー修正不要
  - L70（行頭に'→REM）はコメント行のため除外済み
  - T180（L20初期化）・T150（L40テンポ変更）コマンドはエンジン側でTが無視されるため
    L20の初期化ノートは省略し、DASH_LOOP_TEMPOパラメータで統一制御


■ kcv78 Stage1／Stage2 オリジナルBGM作曲・実装（Claude作曲）

- 【Stage 1 BGM：スペースインベーダー・オマージュ】（Claude作曲）
  - メロディ(O3)＋ハモリ(O2、3度下)＋バス(O1)の3パート・4小節ループ
  - バスは本家インベーダーへのオマージュとして半音下降を8分音符で刻む
  - 敵スピード（speedMul 1〜32、残数に線形連動）を4段階に量子化し、
    段階が上がるたびBGMテンポを上げる（本家の「敵が減るほど速くなる」演出を再現）
    Lv0:T160／Lv1:T140／Lv2:T120／Lv3:T100
  - テンポは加速方向のみ（段階が戻ってもテンポは下げない）。デモ中は変化させない
- 【Stage 2 BGM：プロムナード・アレンジ】（Claude作曲）
  - ムソルグスキー「展覧会の絵」プロムナードの開始動機（ソ・ファ・シ♭・ド・レ）を引用し、
    短調へ傾く独自展開を経て厚い和声で主題回帰する6セグメント・ループ
  - 主旋律(O3)＋内声(O2)＋バス(O1)の3パート、T=110
- BGM切り替え：
  - ゲーム開始（ステージ1）→ playStage1(0)（従来のDash!ワンショットから変更）
  - ステージ2開始 → playStage2()（従来のプロムナード実機MMLから、Claudeアレンジ版へ変更）
  - ステージ3開始 → playDashLoop()（kcv77のDash!フルループ、変更なし）
- スタッフロールの音楽クレジットに「Stage 1／Stage 2　Claude（Anthropic）」を追加
- 旧playDash()／playPromenade()は未使用化（DASH_SEGMENTS/PROM_SEGMENTSの実機解析MMLデータは
  記録として関数ごと保持、コメントで明示）
- 全MMLはparseMMLPart相当で音長検算済み（Stage1・Stage2とも全パート1小節＝96単位で整合）

■ kcv79 アトラクトデモ改善（無駄玉対策・BGM無し・ステージ2クリアで強制終了）

- 【無駄玉対策】命中しない状態が2秒続いたら、狙いを立て直す意味で単純に自機を中央へ戻す挙動を追加
  （demoNoHitTimer。プレイヤー弾が敵またはボスに命中するたびに0リセット、demoActive中は毎フレーム加算。
  2秒以上命中無しでデシジョン分岐に割り込み、desiredAngleを画面中央へ向ける。
  弾を避ける（threat）分岐より優先度は低いまま、ボス追従（bossActive）分岐より優先度を上げて割り込ませた。
  特にボス戦で自機がボスの真下に固定されたまま外れ続け、無駄玉を撃ち続ける問題への対応）
- 【デモ中BGM無し】ステージ2・3突入時のBGM再生（playStage2/playDashLoop）呼び出しをdemoActiveガードで抑制。
  Stage1のテンポアップ（playStage1(lv)再呼び出し）は既存の!demoActiveガードにより元々デモ中は鳴らない仕様だったため対象外
- 【ステージ2クリアで強制終了】デモ中にボス2を撃破しステージ3へ進む直前でデモを打ち切り、
  タイトル→ランキング→デモのアトラクトループへ戻す（ステージ3を見せない要望対応）。
  通常プレイには一切影響しない分岐（if(demoActive)内のみ）

■ kcv80 デモ「無駄玉対策」の不具合修正（中央→最下部中央、持続時間の確保）

- 【不具合修正】kcv79の無駄玉対策が実質無効化されていた問題を修正
  （原因：2秒経過を検知した同フレームでdemoNoHitTimerを即0リセットしていたため、
  中央へ戻る挙動が1フレームだけ発生してすぐ元の分岐（ボス追従等）に戻ってしまい、
  見た目上ほぼ変化が無かった。demoRecenterTimer（新設、DEMO_RECENTER_MS=1500ms）で
  トリガー後の「戻る」状態を1.5秒間維持するよう変更し、実際に位置が動くようにした）
- 【仕様変更】戻り先を画面中央→**最下部中央**（`rows-2`、自機のリセット位置と同じ高さ）に変更
- 命中（敵・ボスいずれも）で demoNoHitTimer・demoRecenterTimer とも即座に解除する挙動は維持
- 検証：headlessでupdateLogic()を直接同期呼び出しし、敵をフレーム毎に除去して命中しない状況を再現。
  2秒経過後にdemoRecenterTimerが1500msから0まで滑らかに減少し、その間に自機が最下部中央
  （x≈cols/2, y≈rows-2）へ実際に収束することを確認。命中時の即時解除、トリガーの再発生（周期的な繰り返し）も確認

■ kcv81 Stage2 ギャラクシアン風隊列リデザイン＋Stage2 BGMアレンジ拡張

- 【Stage2 隊列構成リデザイン】
  - 従来の10×5行（均一）→ギャラクシアン風の4段構成に全面変更
    - 最下段：10列×3行（最弱・青水色 #5be0ff）
    - 第4段：8列×1行（中弱・紫 #c866ff）
    - 第3段：6列×1行（中強・赤 #ff4d5e、護衛の供給源）
    - 最上段：2体（最強・黄 #ffd23b）：左から2列目・右から2列目に各1体
  - 隊列間隔 G2_SPACING=1.35（漢字が読める間隔）
  - 隊列の動き：左右に画面いっぱい移動、上下は sin波で4ドット相当（0.2cell）の緩やかな往復
  - 画面中央配置

- 【ダイブ編隊システム（updateG2Scheduler / updateG2Pending）】
  - 護衛2体が先行し、最強がそのルートをなぞる（護衛→最強の順、200ms間隔でずらして発進）
  - 護衛が撃破済みの場合は穴あきで降下（残存体のみ参加）
  - 発進間隔：G2_DIVE_INTERVAL=2000ms（±500msランダム幅）
  - ダイブ軌道：隊列を離れた瞬間からsin/cosで曲がる。外側に膨らまない
  - 漢字自体も進行方向に向いて回転（rotAngle補間による、既存の回転システムを流用）
  - 画面下・横の画面外に出たら上から戻る周回継続（diveT=0に巻き戻してdiveStartを上端にリセット）
  - 遅延タイマーはsetTimeoutではなくdtの積算で処理（updateG2Pending）

- 【宙返り（最強以外・ランダム）】
  - ダイブ発進時に25%の確率でスケジュール
  - 飛行中途（800〜2000ms後）にlooping状態へ移行
  - cos/sinのeaseInOut補間で元の隊列スロット位置へ帰還（上から戻らず中間で折り返す）
  - 帰還完了でhover状態に戻る

- 【待機中は弾を撃たない】
  - diveState==='hover'の敵はfireTimerを進めない（弾を撃てない）
  - ダイブ中（diving/looping/circling）の敵のみ発砲

- 【最強撃破ボーナス】
  - isBossChar===trueの敵（最強・黄）を撃破すると g2SilenceTimer=2000ms
  - g2SilenceTimer>0の間は全敵の発砲を抑制
  - 画面に「撃破！」ポップアップ表示

- 【残り4体以下で全員周回固定】
  - 待機中（hover）の全敵を circling 状態に移行、隊列に戻らなくなる
  - 攻撃停止ボーナスも永続化（g2SilenceTimerが常に>0の扱い）

- 【最強打ち漏らし】
  - ダイブ中に倒せなかった最強は逃げっぱなし（ステージ2は最上段2体のみで復活なし）
  - 次の編隊は最強抜き（bossPoolが空なら護衛のみ）で発進

- 【最強2体ともダイブ中に生き残ったら】
  - そのままボス戦突入（既存のformationCleared判定には影響しない）

- 【Stage2 BGMアレンジ拡張（STAGE2_SEGMENTS 6→18セグメント）】
  - A部（seg0-3）：主題呈示。プロムナード動機「ソ・ファ・シ♭・ド・レ」を忠実に引用し
    D長調経過を経てより遠い調域まで動機を展開
  - B部（seg4-7）：展開。aマイナーへ転調、半音階的進行で緊張を高め、内声が独立して動く
  - C部（seg8-13）：クライマックス。メロディが1オクターブ跳躍して主題動機が強化されて回帰、
    全パートが厚い和音で盛り上がり、全音符2小節で荘厳な空気（G長調上の保続）
  - D部（seg14-17）：帰還。テンションを静かに落としながらGドミナントで冒頭のC長調へ引き戻し、
    終止形からseg0の「ソ・ファ・シ♭・ド」へ違和感なくループ
  - 全18セグメント・全パートで1小節=96単位の整合を確認済み。STAGE2_TEMPO=110維持

■ 現時点（kcv81）で残っている既知の課題

**実装完了・解消済み：**
- プロムナード「途中で途切れる」→ kcv76でステージ2限定化、kcv78でClaudeアレンジのループBGMに置換し解消
- KANJI_POOL 256文字拡張済み（kcv76）／クリア演出256体対応済み（kcv76）
- ゲームパッド「たまに変な動き」→ kcv75で gp.axes nullチェック追加済み（実機確認待ち）
- Stage1/Stage2のオリジナルBGM実装済み（kcv78）／敵スピード連動テンポ実装済み（kcv78）
- デモの無駄玉対策・デモ中BGM無し・ステージ2クリアで強制終了　→ kcv79で対応、kcv80で無駄玉対策の不具合修正
  （「弾を避けつつ敵を狙う動きへ本格的に作り直す」の代わりに、命中2秒無しで最下部中央へ戻る簡易版で決着）

**報告済みバグ（nogo・着手待ち）：**
- アトラクト無敵モードでクリア → ランキング送信 → タイトル戻却後、アトラクトが開始しない
  （attractMode フラグまわりの状態遷移問題か。修正予定未定）

**保留中（仕様確定後に実装予定）：**
- VEZAR音切れ修正（kcv45）後の実機確認（改善状況の検証）
- VEZAR：タイ処理（同音連続を再トリガーせず伸ばす）は音声エンジン側の構造変更が必要なため
- Dash!フルループ（kcv77）・Stage1/Stage2 BGM（kcv78）の実機での音程・テンポ確認
- 連撃倍率のインフレ強化（8連撃ごとに倍々＝^2）：具体的な数式・上限のすり合わせが必要

**未着手（優先度低または保留）：**
- ステージ1「整列した敵を倒すごとにスピードアップ」仕様漏れの調査
- Firestoreのセキュリティルール未設定（score/clearTimeMsの妥当範囲チェックなど）。
  対応は意図的に保留中（セキュリティが必須となった時点で対応）
- Firestore既存ランキングのscore型混在（Number/String）：新規プレイの蓄積で自然解消を待つ方針
- スワイプで壁を作り弾を跳ね返す案：3案提示済み（A:専用ボタン B:速度閾値 C:キーボード/ゲームパッド限定）、選択待ち
- 弾除けの難易度「難しい」との感想：経過観察中
- 画面下部含む「表示要素の重なり回避」のさらなる全体見直し（HUD以外）
- ハイスコアランキングの詳細要望

■ 主要な決定事項サマリ
- 表示：ゲーム内はすべて漢字（タイトル画面のみカタカナ許可、ランキング画面の
  スコアのみアラビア数字＝v42で変更、意味を持たせた数字列のため）
- スコア：漢数字表示（BigInt対応、京・兆・億まで）。連撃倍率は8連撃をしきい値
  （0.15／1.25）とする方式で確定
- 自機：「自」、1ドット判定、タップ/ドラッグ全方向移動、常時連射。タッチ/クリック
  位置より少し上にオフセット表示
- 敵：画数÷2が耐久力、256種の常用漢字プール（kcv76で拡張）、既出文字は使い切るまで再使用しない
- 音楽：Dash!／VEZAR（PC-E500 PLAY3、基建吉オリジナル）。
  ステージ1：インベーダーオマージュ（Claude作曲・敵スピード連動テンポ）、ステージ2：プロムナードアレンジ（Claude作曲）、ステージ3：Dash!ループ（kcv76〜kcv78で確定）
- ランキング：Firestoreによるグローバルランキング（得点降順→クリアタイム昇順）。
  自己ベスト更新時のみ送信（v35〜）
- ライセンス：MIT
- 公開：v38（kcv38.html）以降が一般公開版。それ以前は非公開の開発版
- クレジット（エンディング画面表示）：
    原案・言葉遊び文化　あやしいわーるど
    監督・企画　基建吉（擬古猫）
    プログラム　Claude（Anthropic）
    音楽　Dash!／VEZAR（PC-E500 PLAY3、基建吉）
    　　　プロムナード（ムソルグスキー「展覧会の絵」より編曲）
    協力　ChatGPT
    デバッグ　ChatGPT／Gemini
    Special Thanks　Gemini／Grok
    © 2026 ZANTEI TECHNOLOGY
